/// @description Initialize

event_user(0);
cam = camera_create_view(x, y, camWidth / camScaleX, camHeight / camScaleY, camAngle, currTarget, -1, -1, camWidth / camScaleX / 2, camHeight / camScaleY / 2);